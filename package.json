{
  "scripts": {
    "start": "grunt",
    "build": "grunt build",
    "publish": "git add --all && git add -u && git commit -m 'Publishing the site' && git push origin master"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-connect": "^0.9.0",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-contrib-watch": "^0.6.1",
    "grunt-markdown": "^0.7.0",
    "jade": "^1.9.1",
    "marked": "^0.3.3",
    "pagemaki": "^0.2.1"
  },
  "dependencies": {
    "lodash": "^3.0.1",
    "moment": "^2.9.0",
    "striptags": "^2.0.1"
  }
}
