extends layout

block body
  div.container.blog-container
    != content

  h3 Blog posts
  ul
    - posts.reverse()
    for post in posts
      li
        a(href=post.url)= post.title
        span= ' '
        small.date= '[' + post.prettyDate + ']'

    - posts.reverse()