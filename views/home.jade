extends layout

block body
  div.container.home-container
    != content

  h3 Recent posts
  ul
    - posts.reverse()
    - posts = posts.slice(0, 5)
    for post in posts
      li
        a(href=post.url)= post.title
        span= ' '
        small.date= '[' + post.prettyDate + ']'